master:
  stage: deploy
  only:
    - master
  tags:
    - nsur-runner-1
  script:
    - cd /home/gitlab-runner/publicwebsite/frontend
    - git reset --hard HEAD
    - rm -rf frontend/package-lock.json
    - git pull
    - npm i --force
    - npm run build
