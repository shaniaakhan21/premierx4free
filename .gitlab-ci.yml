master:
  stage: deploy
  only:
    - master
  tags:
    - nsur-runner-1
  script:
    - cd /home/gitlab-runner/publicwebsite/frontend
    - git reset --hard HEAD
    - git clean -fX
    - git pull
    - npm i --force
    - npm run build
